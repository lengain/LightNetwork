# 不蒜子数据备份系统 - 功能对应表和快速参考

## 🗺️ 功能导航地图

```
┌─────────────────────────────────────────────────────────────────┐
│                    不蒜子数据备份系统                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  📊 数据收集层                                                  │
│  ├─ BUSUANZI对象 (不蒜子官方)                                   │
│  │  └─ site_pv / site_uv / page_pv                             │
│  ├─ busuanzi_backup.js (自动收集)                              │
│  │  └─ 5分钟周期 + 页面关闭时                                  │
│  └─ busuanzi_fallback.js (降级备选)                            │
│     └─ 8秒超时启动                                             │
│                                                                  │
│  💾 存储层                                                       │
│  ├─ localStorage (备份1)                                        │
│  │  └─ 最多30条记录 (~100KB)                                   │
│  ├─ IndexedDB (备份2)                                          │
│  │  └─ 完整历史 (容量充足)                                     │
│  └─ JSON文件 (备份3)                                           │
│     └─ data/busuanzi_stats.json (版本控制)                     │
│                                                                  │
│  🔧 工具层                                                       │
│  ├─ npm run export:busuanzi                                    │
│  │  └─ Node.js导出脚本                                        │
│  ├─ npm run backup:busuanzi                                    │
│  │  └─ 备份管理脚本                                            │
│  └─ scripts/export_busuanzi_puppeteer.js (可选)               │
│     └─ 自动化浏览器导出                                        │
│                                                                  │
│  👥 交互层 (浏览器console)                                       │
│  ├─ busuanziShowBackupSummary()                                │
│  │  └─ 查看数据摘要                                            │
│  ├─ busuanziViewBackup()                                       │
│  │  └─ 查看所有备份                                            │
│  ├─ busuanziExportBackup()                                     │
│  │  └─ 下载备份文件                                            │
│  └─ busuanziClearBackup()                                      │
│     └─ 清除本地数据                                            │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📋 快速参考表

### 🔧 Web开发者命令 (在网站上执行)

| 命令 | 作用 | 输出 | 何时使用 |
|------|------|------|---------|
| `busuanziShowBackupSummary()` | 显示备份摘要 | TABLE | 快速查看数据 |
| `busuanziViewBackup()` | 查看完整备份 | TABLE | 详细检查历史 |
| `busuanziExportBackup()` | 下载JSON文件 | 下载 | 本地备份 |
| `busuanziClearBackup()` | 清除本地数据 | LOG | 重置备份 |

### 💻 服务器命令 (终端执行)

| 命令 | 文件 | 作用 | 输出 |
|------|------|------|------|
| `npm run export:busuanzi` | scripts/export_busuanzi.js | 导出备份 | JSON |
| `npm run backup:busuanzi` | scripts/backup_busuanzi.js | 管理备份 | LOG |
| `hexo generate` | Hexo | 生成网站 | HTML |
| `hexo deploy` | Hexo | 部署网站 | 部署日志 |

---

## 🎯 使用场景速查表

### 场景1：我刚部署了网站

**目标**: 保存当前的统计数据

**执行步骤**:
```bash
# 1️⃣  导出数据
npm run export:busuanzi

# 2️⃣  查看是否成功
ls -lh data/busuanzi_stats.json

# 3️⃣  提交到git
git add data/
git commit -m "备份不蒜子统计数据"
git push
```

**验证**: 
```javascript
busuanziShowBackupSummary()  // 应该看到有数据
```

---

### 场景2：我想检查网站的浏览统计数据

**目标**: 查看网站总PV、UV等

**执行步骤**:
1. 打开网站
2. 打开浏览器 F12 → Console
3. 执行: `busuanziShowBackupSummary()`
4. 查看表格输出

**预期结果**:
```
网站总浏览数 (PV)    | 12345
网站总访客数 (UV)    | 5000
当前页面浏览数       | 42
...
```

---

### 场景3：不蒜子服务不可用，我要查看历史数据

**目标**: 确保能显示备份数据

**自动过程**:
1. 打开网站
2. 不蒜子失败 → Fallback自动启动（8秒）
3. 备份数据自动显示（带*标记）
4. 用户体验不中断 ✅

**手动验证**:
```javascript
busuanziViewBackup()  // 查看所有备份记录
```

---

### 场景4：我想下载备份数据为文件

**目标**: 得到JSON格式的备份文件

**执行步骤**:
1. 打开网站
2. F12 → Console
3. 执行: `busuanziExportBackup()`
4. 文件自动下载

**文件位置**: 下载文件夹中的 `busuanzi_backup_*.json`

---

### 场景5：本地localStorage已损坏，我要清空重新收集

**目标**: 重置所有本地数据

**执行步骤**:
```javascript
busuanziClearBackup()  // 清除所有备份
```

然后关闭刷新网站，等待5分钟让系统重新收集数据。

---

### 场景6：我要自动化每周备份

**目标**: 设置定期导出脚本

**执行步骤**:

编辑 `package.json` 的 deploy 脚本:
```json
{
  "scripts": {
    "deploy": "hexo generate && hexo deploy && npm run export:busuanzi"
  }
}
```

然后使用 npm 的 `pre-` 和 `post-` 钩子：
```bash
# 创建脚本
cat > scripts/post-deploy.js << 'EOF'
// 部署后自动导出
require('./export_busuanzi.js');
EOF

# 在 package.json 中添加
npm run deploy  # 现在会自动导出备份
```

---

## 📂 文件位置快速查询

### 前端脚本（用户浏览器加载）
```
source/
├── js/
│   ├── busuanzi_backup.js       ← 数据收集脚本
│   └── busuanzi_fallback.js     ← 降级脚本
└── _data/
    └── body-end.swig             ← 脚本引入配置
```

### 服务器脚本（Node.js执行）
```
scripts/
├── export_busuanzi.js           ← 基础导出
├── backup_busuanzi.js           ← 备份管理
└── export_busuanzi_puppeteer.js ← 高级导出
```

### 数据备份（保存的数据）
```
data/
├── busuanzi_backup.json         ← 动态备份
└── busuanzi_stats.json          ← 导出备份
```

### 配置文件（不需要修改）
```
themes/next/
└── _config.yml                  ← bodyEnd已配置

package.json                      ← npm脚本已添加
```

### 文档（参考资料）
```
./
├── BUSUANZI_QUICK_START.md       ← 快速开始（必读）
├── BUSUANZI_BACKUP_README.md     ← 技术文档
├── IMPLEMENTATION_COMPLETE.md    ← 项目总结
└── FUNCTION_REFERENCE.md         ← 本文件
```

---

## 🔄 数据流向图

```
不蒜子JS加载 (busuanzi.ibruce.info)
    ↓ (每5分钟/页面关闭)
BUSUANZI对象
    ├─→ busuanzi_backup.js
    │   ├─→ localStorage
    │   └─→ IndexedDB
    │
    └─→ 浏览器显示
         ↓ (超时8秒)
    busuanzi_fallback.js
         └─→ 显示备份数据

NPM脚本
    ├─ npm run export:busuanzi
    │  └─→ data/busuanzi_stats.json
    │
    └─ npm run backup:busuanzi
       └─→ 备份管理
```

---

## 🎯 功能对应关系表

| 需求 | 使用工具 | 命令 | 返回值 |
|------|---------|------|--------|
| **查看当前数据** | 浏览器console | `busuanziShowBackupSummary()` | 表格 |
| **查看历史记录** | 浏览器console | `busuanziViewBackup()` | 表格 |
| **导出为文件** | 浏览器console | `busuanziExportBackup()` | JSON下载 |
| **清除数据** | 浏览器console | `busuanziClearBackup()` | LOG |
| **服务器导出** | 终端 | `npm run export:busuanzi` | JSON文件 |
| **备份管理** | 终端 | `npm run backup:busuanzi` | LOG |
| **生成网站** | 终端 | `hexo generate` | HTML |
| **部署网站** | 终端 | `hexo deploy` | 日志 |

---

## 🚨 故障快速修复表

| 问题 | 症状 | 原因 | 解决方案 |
|------|------|------|---------|
| 没看到统计数字 | 页面无数字显示 | 不蒜子加载失败 | 等8秒自动显示备份 |
| 想查看数据 | 不知道怎么看统计 | 不知道命令 | 运行 `busuanziShowBackupSummary()` |
| localStorage满了 | 警告或错误 | 数据太多 | 运行 `busuanziClearBackup()` |
| 想重新开始 | 需要清空所有数据 | 历史数据混乱 | 运行 `busuanziClearBackup()` |
| 想导出备份 | 需要JSON文件 | 不知道方法 | 运行 `busuanziExportBackup()` |
| 想查看完整历史 | 需要看所有记录 | 不知道命令 | 运行 `busuanziViewBackup()` |

---

## 💡 高级技巧

### 技巧1：定期自动导出
创建 GitHub Actions 工作流:
```yaml
# .github/workflows/export-busuanzi.yml
name: Export Busuanzi Data
on:
  schedule:
    - cron: '0 0 * * 0'  # 每周一午夜
jobs:
  export:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
      - run: npm ci && npm run export:busuanzi
      - uses: EndBug/add-and-commit@v7
```

### 技巧2：在git钩子中自动导出
```bash
# .git/hooks/post-commit
#!/bin/bash
npm run export:busuanzi
git add data/busuanzi_stats.json
git commit --amend --no-edit
```

### 技巧3：生成数据报告
```javascript
// 在console中执行
const backup = busuanziViewBackup();
console.log('过去7天平均PV:', calculateAverage(backup));
console.log('PV增长率:', calculateGrowth(backup));
```

---

## 📊 性能指标

| 指标 | 值 | 说明 |
|------|-----|------|
| 脚本总大小 | ~14KB | 所有JS脚本 |
| localStorage占用 | 100-200KB | 30条记录 |
| IndexedDB占用 | 可扩展 | 容量充足 |
| 检查间隔 | 500ms | 等待BUSUANZI |
| 收集间隔 | 5分钟 | 定期收集 |
| 超时时间 | 8秒 | Fallback启动 |

---

## ✅ 日常维护清单

### 每日
- [ ] 网站正常显示统计数字

### 每周
- [ ] 运行 `npm run export:busuanzi`
- [ ] 将备份文件提交到git

### 每月
- [ ] 检查data目录文件大小
- [ ] 运行 `busuanziShowBackupSummary()` 确认有数据
- [ ] 查看备份文件内容

### 每季度
- [ ] 清理过期备份（可选）
- [ ] 更新文档（如有变化）
- [ ] 测试fallback机制

---

## 📞 获取帮助

### 查看"?"帮助
```javascript
// 在console中输出所有可用命令的说明
console.log(`
可用命令:
1. busuanziShowBackupSummary()    - 显示备份摘要
2. busuanziViewBackup()            - 查看所有备份
3. busuanziExportBackup()          - 导出为JSON文件
4. busuanziClearBackup()           - 清除本地备份
`);
```

### 检查脚本加载状态
```javascript
// 检查不蒜子是否加载
console.log('BUSUANZI:', typeof BUSUANZI);

// 检查localStorage
console.log('localStorage:', localStorage.getItem('busuanzi_backup_data'));
```

---

## 🎓 学习资源

- 📖 完整文档: [BUSUANZI_BACKUP_README.md](BUSUANZI_BACKUP_README.md)
- 🚀 快速开始: [BUSUANZI_QUICK_START.md](BUSUANZI_QUICK_START.md)
- 📋 项目总结: [IMPLEMENTATION_COMPLETE.md](IMPLEMENTATION_COMPLETE.md)
- 📚 本参考表: [FUNCTION_REFERENCE.md](FUNCTION_REFERENCE.md) (当前文件)

---

**这是一个快速参考指南！**  
如需详细说明，请查阅其他文档文件。

*最后更新: 2026年2月28日*
